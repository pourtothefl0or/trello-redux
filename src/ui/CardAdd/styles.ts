import styled from 'styled-components';
import { COLORS, PRIMARY } from '../../constants';

export const StyledCardAdd = styled.button`
  border: 1px dashed ${COLORS.topaz};
  border-radius: ${PRIMARY.border};
  padding: 10px;
  width: 100%;
  font-size: 14px;
  color: ${COLORS.topaz};
`;
